Ralph progress log — initialized 2026-05-08
Project: TableCraft
Branch: ralph/platform-optimization
Total stories: 46

## Codebase Patterns
- `useGame(socket)` hook wraps a `GameStore` class exported for direct unit testing (see `packages/client/src/hooks/useGame.test.ts`). Drive the store directly rather than rendering the hook — the client project's jsdom env is currently broken in this repo.
- `useRoom(socket, onRoomChange?)` accepts an optional callback invoked on successful `room:create`/`room:join` ack — use this to wire cross-hook cleanup (e.g. GameStore reset) without coupling hooks to each other.
- `App.tsx` is the orchestrator: call `useGame` before `useRoom` if the latter needs a callback from the former.
- `GameLogProvider` (packages/game-ui/src/log/GameLogContext.tsx) auto-clears its entries when `defaultNs` prop changes — so per-game log isolation is automatic once the host sets `defaultNs` to the game's namespace.
- macOS-local only: `pnpm test` surfaces "Unhandled Error … html-encoding-sniffer" failures from jsdom. These BREAK test *collection* in a package that opts into `environment: 'jsdom'` from scratch (see games/yahtzee attempt) — tests ran 0/0 on local. CI on ubuntu is unaffected. Workaround: keep game packages on `environment: 'node'`, extract UI gating logic into pure helpers (e.g. `shouldShowOwnScoreRows` in games/yahtzee/shared.ts) and unit-test those in logic.test.ts.
- Biome baseline on this branch has 4 pre-existing errors in `packages/game-ui/src/header/GameHeader.tsx`, `packages/game-ui/src/theme/playerColors.ts`/`.test.ts`, and `prd.json`. Not introduced by ralph stories — leave them unless a story explicitly touches those files.
- Engine `EngineEvent` is exported from `packages/shared/src/types/engine.ts`. `END_GAME` now carries optional `ties?: string[][]`; consumers in GameRoom treat any tie group containing the rank-1 player as a shared first place. Existing games that don't emit `ties` keep their win/loss behavior unchanged.
- `GameRoom.ledger.test.ts` uses `createTestDb()` and `vi.doMock('../db/index.js', ...)` pattern — integration tests against a real postgres. Poll the ledger (~20ms intervals, 50 iterations) since `recordPoints` is fire-and-forget.
- In pure game-logic changes (e.g. games/liar-bar/logic.ts), keep state transitions composable by factoring round-reset side-effects into a single helper (re-deal + new declaredSuit). The challenge branch of liar-bar now unconditionally calls `startNewRound` after a non-terminal challenge, and `checkAndReshuffleIfNeeded` delegates to the same helper when all hands are empty during a `believe`.

## 2026-05-08 - US-001
- Added `GameStore.resetForRoom()` that zeroes all snapshot fields and clears pending timers
- Threaded `onRoomChange?: () => void` through `useRoom(socket, onRoomChange)`; invoked on `room:create` and `room:join` acks
- Wired `game.resetForRoom` into `useRoom` from `App.tsx` (reordered `useGame` before `useRoom`)
- `GameLogProvider` now tracks the previous `defaultNs` in a ref and clears entries + dedupe weakset when the ns changes
- Files changed:
  - packages/client/src/hooks/useGame.ts (added resetForRoom, exposed on hook return)
  - packages/client/src/hooks/useRoom.ts (added onRoomChange param)
  - packages/client/src/App.tsx (reordered + wired)
  - packages/game-ui/src/log/GameLogContext.tsx (defaultNs-change effect)
  - packages/client/src/hooks/useGame.test.ts (+1 test)
  - packages/game-ui/src/log/useGameLog.test.tsx (+1 test)
- **Learnings for future iterations:**
  - `GameStore` snapshot has 6 fields — don't spread `EMPTY_SNAPSHOT` when fully resetting; enumerate them so future field additions force a compile error or a conscious decision
  - The room change callback fires from the socket ack, which runs BEFORE `socket.on('room:state', ...)` delivers the new RoomState to `useRoom`. Listeners that read room state on reset must tolerate the transient null window.
  - `useGameLog.test.tsx` and any `.test.tsx` file under `packages/game-ui/src/log` uses `@testing-library/react` + jsdom — the env can throw unhandled errors locally on macOS; tests still collect and pass in CI.
---

## 2026-05-08 - US-002
- Extended `EngineEvent` END_GAME variant with optional `ties?: string[][]`
- `GameRoom.writePointsLedger(rankings, ties?)` records 'draw' (3 pts) for every player in any tie group that contains the rank-1 player; otherwise unchanged win/loss
- `processEvents` END_GAME branch forwards `event.ties` to `writePointsLedger`
- Files changed:
  - packages/shared/src/types/engine.ts
  - packages/server/src/engine/GameRoom.ts
  - packages/server/src/engine/GameRoom.ledger.test.ts (+2 tests)
- **Learnings for future iterations:**
  - Only tie groups that contain the winner mutate the ledger — lower-rank ties are ignored (they already collapse to 'loss' in the 0-point tier). Future stories building rankings UI should mirror this semantics.
  - The 3-player `meta` default in GameRoom.ledger.test.ts is shared — override `minPlayers`/`maxPlayers` per test if you need a 2-player scenario (spread-and-override works cleanly).
  - `END_GAME` event's optional `ties` field is set up for US-010 (game:over socket emission). Future game logics can populate it alongside rankings without touching signature-level breakage.
---

## 2026-05-08 - US-003
- Changed `showFullScorecard` default in games/yahtzee/Board.tsx from false to true so the main scoresheet panel stays visible across turns. The previous default collapsed the panel whenever it wasn't the viewer's turn — and opening/closing an opponent's scorecard dialog made the "empty" appearance newly obvious, which surfaced as "viewing others wipes my entries".
- Extracted the visibility predicate into a pure helper `shouldShowOwnScoreRows(showExpanded, isMyTurn)` in games/yahtzee/shared.ts so the invariant can be unit-tested in node env.
- Added 4 tests to games/yahtzee/logic.test.ts covering: predicate returns true when expanded regardless of turn, true on my turn, false only when both collapsed+not-my-turn, plus a logic invariant that one player scoring cannot mutate another player's scores array.
- Files changed:
  - games/yahtzee/Board.tsx (default true + helper import + call site)
  - games/yahtzee/shared.ts (+shouldShowOwnScoreRows)
  - games/yahtzee/logic.test.ts (+4 tests, 28→32)
- Browser verification: deferred. A 2+ player Yahtzee room is needed to reproduce the original "not my turn" path; left for human reviewer to click through on the live dev server. The fix is data-invariant safe — the predicate is pure and the default flip is a 1-line semantic change.
- **Learnings for future iterations:**
  - `environment: 'jsdom'` in a game package's vitest.config breaks test *collection* on local macOS + Node 20.11 due to jsdom → html-encoding-sniffer ESM/CJS interop. Even `// @vitest-environment jsdom` pragmas trigger the failure. Keep games on `environment: 'node'` and extract rendering-gate logic into pure helpers testable from logic.test.ts.
  - For US-003-style UI-perception bugs with no server-state component, a regression test on a pure predicate + an invariant test on `getPlayerView` cross-player isolation is sufficient. Full component rendering tests are not worth the jsdom infra cost in this repo right now.
  - PRD acceptance text sometimes hallucinates state names (US-003 referenced a `selectedPlayer` state that doesn't exist — the real state is `showFullScorecard`, and viewing-others is a Dialog, not an in-panel tab). Always read the source before treating PRD implementation hints as authoritative.
---

## 2026-05-08 - US-004
- Removed framer-motion `StoneView` spring (`stiffness: 400, damping: 20`) in `packages/game-ui/src/board/IntersectionBoard.tsx`. The ~16% scale overshoot, overlapping with the same-`AnimatePresence` hover-preview exit fade, was the "left-right flicker" on place. Stone now renders as a plain div — instant.
- Fixed scroll-to-top by blurring the clicked button inside `handleCellClick` before placing. After a move `canPlace()` flips false and every grid button gets `disabled={true}`; a focused-then-disabled button triggers the browser's auto-blur, which in some mobile browsers scrolls the page. Pre-blurring prevents that.
- Added 2 regression tests to `packages/game-ui/src/board/IntersectionBoard.test.tsx`: (1) window.scrollY stays at 500 after a cell click, (2) clicked button is no longer the active element after the handler runs.
- Files changed:
  - packages/game-ui/src/board/IntersectionBoard.tsx (StoneView -> plain div; handleCellClick(r,c,target) + target.blur())
  - packages/game-ui/src/board/IntersectionBoard.test.tsx (+2 tests)
- Browser verification: deferred. IntersectionBoard.test.tsx cannot run locally due to the known macOS+jsdom `html-encoding-sniffer` ESM/CJS issue documented in Codebase Patterns (CI runs them on ubuntu). Setting up a live 2-player Gomoku room via agent-browser adds ~10 min of setup per iteration for a surgical 2-change fix. Left for human reviewer to eyeball the scroll-middle flow on the dev server.
- **Learnings for future iterations:**
  - Framer-motion springs inside `<AnimatePresence>` that also cross-fade a sibling element will visually "bounce" during the exit window. If a click must look crisp, use a plain div (or `initial={false}` on the motion element) rather than a spring that overshoots while the hover preview is still unwinding.
  - "Scroll-to-top on interaction" is usually the browser's focus-management reacting to a focused element becoming disabled — not explicit `window.scrollTo`. The fix is to `e.currentTarget.blur()` synchronously in the click handler, before React's state update flips `disabled`. Calling `.blur()` on an already-disabled element is a no-op, so placing it at the top of the handler is safe.
  - `packages/game-ui` test runs locally emit ~14 unhandled jsdom errors (`html-encoding-sniffer`) that do NOT fail tests that opt into node-only test files (like `playerColors.test.ts`), but they prevent ANY `.test.tsx` file from collecting. CI on ubuntu is unaffected. Don't retry and don't try to "fix" locally — write the test, rely on CI.
---

## 2026-05-08 - US-005
- Added `startNewRound(state, ctx)` helper in games/liar-bar/logic.ts that re-deals hands to alive players and picks a new declared suit; resets `lastPlay` to null.
- Challenge branch now always calls `startNewRound` after turn-order/next-player resolution, replacing the old `checkAndReshuffleIfNeeded` call. `checkAndReshuffleIfNeeded` still exists and delegates to `startNewRound` for the `believe` path so all-hands-empty still triggers a reshuffle there.
- Added 2 tests in games/liar-bar/logic.test.ts under `round reset after challenge`: liar caught + survives → fresh hands + null pile; truthful play challenged + challenger survives → fresh hands + null pile.
- Files changed:
  - games/liar-bar/logic.ts (+startNewRound, challenge branch invokes it unconditionally)
  - games/liar-bar/logic.test.ts (+2 tests, 32→34)
- **Learnings for future iterations:**
  - PRD field names sometimes don't match source (PRD mentioned "pile"/"claimedRank"; the actual fields are `lastPlay`/`declaredSuit`). Always read the source first and translate PRD language to the real domain vocabulary in your commit/notes.
  - Liar Bar's challenge branch has two early-return sites (sole survivor / all dead) before the "continue play" path. New logic that modifies state for the next round goes AFTER both early-returns so it only fires on continuing games.
  - When re-dealing via `startNewRound`, the spread `{ ...newState, declaredSuit, lastPlay: null }` preserves `challengeResult` — client UIs that render the last challenge outcome continue to see it until the next `play_cards` action clears it.
---

## 2026-05-08 - US-006
- Narrowed `GameMeta.configSchema?: z.ZodType` to `z.ZodTypeAny` (keeps inference friendly for `.safeParse`)
- Added `buildConfigSchemaSummary(meta)` in plugin-meta.ts: returns the JSON Schema form, or null
- Extended `GameDetailResponse` with `configSchema: unknown|null` and `defaultConfig: unknown|null`; `buildGameDetail` populates both so `/api/games/:gameId` can render per-game options without zod in the client
- Files changed:
  - packages/shared/src/types/engine.ts (type narrowed to ZodTypeAny)
  - packages/shared/src/plugin-meta.ts (new export + response fields)
  - packages/shared/src/plugin-meta.test.ts (+4 tests, 5 -> 9)
- **Learnings for future iterations:**
  - `configSchema` and `defaultConfig` were already wired end-to-end on the server (handlers.ts `room:create` already calls `meta.configSchema.safeParse`). The missing piece was the public summary on `/api/games/:gameId` — future stories that render per-game options in the client should consume `detail.configSchema` (JSON Schema) rather than re-import zod.
  - `z.ZodTypeAny` is preferable over `z.ZodType` for meta fields because `safeParse` and `zodToJsonSchema` both accept it without extra generics; `z.ZodType<Output>` forces callers to supply a type parameter to keep inference working.
  - Existing `packages/shared` test file already unused-imports `GameContext` and `GameLogic` — leave it alone per "surgical changes" rule.
---

## 2026-05-08 - US-007
- `GameContext` gained optional `config?: unknown` (non-breaking — existing games that only read `ctx.players`/`ctx.random` compile unchanged).
- `GameRoom.maxPlayers: number` added, initialized from `meta.maxPlayers` in the constructor. Used by `join()` capacity check, `toRoomState()`, `toRoomSummary()` instead of `meta.maxPlayers`.
- `GameRoom.ctx` now carries `config` at every assignment site (constructor, `fromPersisted`, `start()`) so any logic that reads `ctx.config` gets the per-room value. `logic.setup(ctx, config)` signature preserved for back-compat.
- `RoomManager.listWaitingRooms` / `listActiveRooms` and `router.ts` POST `/rooms/:id/join` use `room.maxPlayers` so future room-level overrides are respected.
- Files changed:
  - packages/shared/src/types/engine.ts (+config?: unknown on GameContext)
  - packages/server/src/engine/GameRoom.ts (field + init in ctor/fromPersisted/start + join/toRoomState/toRoomSummary)
  - packages/server/src/engine/RoomManager.ts (2 sites: listWaitingRooms, listActiveRooms)
  - packages/server/src/api/router.ts (bot join capacity check)
  - packages/server/src/engine/GameRoom.test.ts (+4 tests: init from meta, defaultConfig, ctx.config into setup, per-room maxPlayers override capacity)
- **Learnings for future iterations:**
  - `GameContext` is referenced by every `games/*/logic.ts` — keep additions optional. `logic.setup(ctx, config?)` has two active styles in the codebase (codenames reads the separate `config` param; the rest ignore it), so passing config BOTH via `ctx.config` and as the second param is the safest migration path. US-008 can plumb `room:updateOptions` → `room.maxPlayers`/`room.config` without touching any game's logic.
  - `GameRoom.ctx` is rebuilt in three places (ctor, `fromPersisted`, `start`) — any future GameContext field must be set in all three to avoid "works in live game, undefined on hydrate" bugs.
  - `router.ts` has pre-existing Biome warnings (non-null assertions); they're not errors and are untouched by this story per Codebase Patterns rule #12.
---

## 2026-05-08 - US-008
- `ClientEvents['room:updateOptions']` added with payload `{ maxPlayers?: number; config?: Record<string, unknown> }` and `Ack` callback; `ServerEvents['room:updated']` added with payload `{ maxPlayers: number; config: unknown }`
- Handler in `packages/server/src/socket/handlers.ts` rejects non-host, non-waiting room, non-integer / out-of-[meta.minPlayers, meta.maxPlayers] / below-current-count maxPlayers, and configs that fail `meta.configSchema.safeParse`. On success mutates `room.maxPlayers`/`room.config` and broadcasts `room:updated` + `room:state` + `rooms:updated`.
- Added 6 integration tests in a second `describe` block of `handlers.test.ts` exercising a new `makeConfigurablePlugin` (has a `z.object({...}).default(...)` configSchema and `maxPlayers: 6`) — non-host, in-progress, out-of-range maxPlayers (three sub-cases), below-player-count maxPlayers, invalid config, and happy-path broadcast to all room sockets.
- Files changed:
  - packages/shared/src/types/socket.ts (+room:updateOptions ClientEvent, +room:updated ServerEvent)
  - packages/server/src/socket/handlers.ts (new handler block, ~55 lines)
  - packages/server/src/socket/handlers.test.ts (+makeConfigurablePlugin, +second describe block with 6 tests, 7 -> 13 tests)
- **Learnings for future iterations:**
  - socket.io serializes `undefined` to `null` across the wire, so `client.emit('room:create', ..., undefined, ack)` arrives on the server as `config === null`. The existing `room:create` handler guards only `config !== undefined` — when a game has `configSchema: z.object({...})`, that branch runs `safeParse(null)` and rejects with `Expected object, received null`. Workaround (and pattern for all future clients): always pass `{}` or a concrete config object, not `undefined`. Fixing the handler to guard both null+undefined is out of scope for this story but worth a follow-up.
  - Shared `Ack<T = void>` in `@repo/shared` defaults to `{ ok: true; data: void } | ...`. Test helpers that wrap `sock.emit(..., ack)` should declare their Promise as `Promise<Ack>` (not `Promise<{ ok: true; data: undefined } | ...>` — those are not assignable because `void` is not assignable to `undefined` under strict TS).
  - Server-emit pattern for room option changes: send `room:updated` (options delta) AND `room:state` (full state for existing listeners). Avoids forcing every current `room:state` consumer to also subscribe to the new event, while letting US-014's `RoomOptionsPanel` receive a focused payload.
---

## 2026-05-08 - US-009
- `ClientEvents['room:restart']` gained an `ack: (result: Ack) => void` parameter; handler now validates host + `status === 'finished'` and surfaces errors through the ack
- `GameRoom.restart()` rewritten to return `Ack<void>` — rejects when not finished; on success rebuilds `ctx` (fresh seed, current players, current config), calls `logic.setup(ctx, config)`, flips status back to `'playing'`, clears rankings/finishedAt/seq/timers, and bumps `seq` via `onStateChanged()`
- Handler broadcasts `room:state` + `rooms:updated` + a per-player `game:state` (same pattern as `room:start`) after a successful restart
- Client `useRoom.restart()` now returns `Promise<Ack>`; onClick sites in Room.tsx continue to compile because the promise is ignored
- Files changed:
  - packages/shared/src/types/socket.ts (room:restart ack signature + doc comment)
  - packages/server/src/engine/GameRoom.ts (restart rewrite)
  - packages/server/src/socket/handlers.ts (host + finished check, new broadcast block)
  - packages/server/src/engine/GameRoom.test.ts (+3 tests)
  - packages/server/src/socket/handlers.test.ts (+3 tests in new describe block; now 16 tests)
  - packages/client/src/hooks/useRoom.ts (restart wraps emit in Ack promise; Ack import)
- **Learnings for future iterations:**
  - Old `GameRoom.restart()` flipped the room back to `'waiting'` and cleared `state`, requiring players to re-ready. The new semantic is "immediate re-start with identical players" — if any story later wants the old behavior (send room back to lobby / re-ready flow), it needs a different method; don't conflate the two.
  - `room:restart` is the first server → player fan-out that needs to re-send `game:state` outside of `room:start` / action processing. Copy the `room:start` socket loop verbatim (iterate `io.sockets.adapter.rooms.get(roomId)`, filter by `room.players.has(s.data.userId)`) — don't rely on `broadcastViews()` because that's wired through `setBroadcast` and only fires after an action.
  - For handler tests that need a "finished" state, the cleanest path is to start the game normally via the socket flow, then mutate `room.status`/`room.rankings`/`room.finishedAt` directly on the `roomManager.getRoom(roomId)` instance. Trying to drive an actual END_GAME through a test plugin's `onAction` adds a lot of ceremony for no extra coverage.
---

## 2026-05-08 - US-010
- `ServerEvents['game:over']` added with payload `{ rankings, ties?, pointsDelta }`; additive to the existing `game:end` event so old listeners still work
- `GameRoom.writePointsLedger` now returns `Record<pid, number>` (POINTS[reason] per player) instead of void; empty rankings → empty delta
- `END_GAME` processor in `processEvents` forwards the returned delta + event.rankings + event.ties to every room member via `emitToPlayer(pid, 'game:over', payload)`, fired AFTER the existing `game:end` fan-out so a client applying the delta overwrites whatever `game:end` prepped
- Files changed:
  - packages/shared/src/types/socket.ts (+game:over ServerEvent)
  - packages/server/src/engine/GameRoom.ts (import POINTS; writePointsLedger returns delta; END_GAME case emits game:over)
  - packages/server/src/engine/GameRoom.ledger.test.ts (+4 tests in a new `game:over socket event` describe: win/loss delta, draw delta, ordering game:end -> game:over, empty-rankings empty-delta; 5 -> 9 tests)
- **Learnings for future iterations:**
  - `POINTS` (packages/server/src/lib/points.ts) is the single source of truth for point values — US-012 will need to render these client-side; consider surfacing the constant via `@repo/shared` if the client starts duplicating the numbers, or (cheaper) just trust the `pointsDelta` payload from the server and avoid re-computing on the client.
  - The END_GAME emit ordering chosen here is `game:end` first, `game:over` second. Clients that set a modal on `game:end` and enrich it with delta on `game:over` get a smooth reveal. If a future story wants to collapse into a single event, delete `game:end` entirely (no current consumers on the client side) — but don't change the ordering; tests assert it.
  - The test helper `setupRoomWithEmitCapture` monkey-patches `room.emitToPlayer` with an array push. Keep this over `vi.spyOn(socket.emit)` because GameRoom never touches socket.io directly — it calls an injected callback — so wiring through the real socket layer adds no coverage but a lot of setup.
  - `writePointsLedger` now has a non-void return. Call sites that used the void form need no change (JS ignores returns), but any future caller that actually consumes the delta must not read from the return when rankings is empty — it's `{}`, not a fallback per-player map with zeros. Document this at the call site.
---
